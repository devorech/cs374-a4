The following text below contains the necessary information to run and compile the program!
________________________________________________________________________________________________________

STEPS TO RUN AND COMPILE:

1. To compile the program, enter the following into the command line:
    gcc -std=gnu99 -pthread -g -o line_processor main.c

2. You should now have the file "line_processor" in your current directory

3. To run the program, enter the following into the command line: 
    ./line_processor
